<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="10dp"
    android:paddingBottom="10dp">

    <!-- Artwork on the left -->
    <ImageView
        android:id="@+id/widget_artwork"
        android:layout_width="54dp"
        android:layout_height="54dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:background="@drawable/widget_artwork_background"
        android:scaleType="centerCrop"
        android:src="@drawable/ic_widget_placeholder"
        android:contentDescription="Episode Artwork" />

    <!-- Controls on the right -->
    <LinearLayout
        android:id="@+id/widget_controls_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageButton
            android:id="@+id/widget_btn_rewind"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="@drawable/widget_button_background"
            android:contentDescription="Rewind"
            android:padding="7dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_widget_replay_10" />

        <FrameLayout
            android:layout_width="44dp"
            android:layout_height="44dp"
            android:layout_marginStart="4dp"
            android:layout_marginEnd="4dp">

            <ImageButton
                android:id="@+id/widget_btn_play_pause"
                android:layout_width="44dp"
                android:layout_height="44dp"
                android:background="@drawable/widget_play_button_background"
                android:contentDescription="Play or Pause"
                android:padding="10dp"
                android:scaleType="fitCenter"
                android:src="@drawable/ic_widget_play" />

            <ProgressBar
                android:id="@+id/widget_buffering_spinner"
                style="?android:attr/progressBarStyleSmall"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:indeterminate="true"
                android:indeterminateTint="#FFFFFFFF"
                android:contentDescription="Buffering audio"
                android:visibility="gone" />
        </FrameLayout>

        <ImageButton
            android:id="@+id/widget_btn_fast_forward"
            android:layout_width="38dp"
            android:layout_height="38dp"
            android:background="@drawable/widget_button_background"
            android:contentDescription="Fast Forward"
            android:padding="7dp"
            android:scaleType="fitCenter"
            android:src="@drawable/ic_widget_forward_30" />
    </LinearLayout>

    <!-- Title and Subtitle in the middle -->
    <LinearLayout
        android:id="@+id/widget_info_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_toEndOf="@id/widget_artwork"
        android:layout_toStartOf="@id/widget_controls_container"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="8dp"
        android:orientation="vertical">

        <TextView
            android:id="@+id/widget_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Podcast Merlin"
            android:textColor="#FFFFFFFF"
            android:textSize="14sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/widget_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Tap to open player"
            android:textColor="#B3FFFFFF"
            android:textSize="12sp" />
    </LinearLayout>

    <!-- Bottom Progress Bar -->
    <ProgressBar
        android:id="@+id/widget_progress_bar"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_alignParentBottom="true"
        android:max="100"
        android:progress="0"
        android:progressDrawable="@drawable/widget_progress_drawable" />
</RelativeLayout>
